<%= form_for(@blog_page) do |f| %>
  <% if @blog_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_page.errors.count, "error") %> prohibited this blog_page from being saved:</h2>

      <ul>
      <% @blog_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
