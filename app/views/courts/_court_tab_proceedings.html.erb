<div class="tab-pane fade active in" id="proceedings">
  <div class="row">
    <table>
      <thead>
        <tr>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @proceedings.each do |proceeding| %>
          <%= render proceeding %>
        <% end %>

        <%= paginate @proceedings.all %>
      </tbody>
    </table>
    Work in progress
  </div>

</div>
