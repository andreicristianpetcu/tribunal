<h2>Instanțe</h2>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courts.each do |court| %>
      <%= render court %>
    <% end %>

    <%= paginate @courts.all, :param_name => :courts_page %>>
  </tbody>
</table>
