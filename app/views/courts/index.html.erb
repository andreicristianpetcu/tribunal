<h1>Instanțe</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render :collection => @courts, :partial => "court"%>
    <%= paginate @courts.all %>
  </tbody>
</table>
