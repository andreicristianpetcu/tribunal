<h1>Instanțe</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courts.each do |court| %>
      <%= render court %>
    <% end %>

    <%= paginate @courts.all %>
  </tbody>
</table>
