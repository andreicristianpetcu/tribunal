<%= form_for(@trial_court) do |f| %>
  <% if @trial_court.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trial_court.errors.count, "error") %> prohibited this court from being saved:</h2>

      <ul>
      <% @trial_court.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
